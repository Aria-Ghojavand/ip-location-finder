apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-app.fullname" . }}-test"
  labels:
    {{- include "go-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-app.fullname" . }}:{{ .Values.service.port }}']
    - name: curl-health
      image: curlimages/curl
      command: ['curl']
      args: ['{{ include "go-app.fullname" . }}:{{ .Values.service.port }}/health']